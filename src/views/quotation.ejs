<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Quotation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        font-size: 12px;
        margin: 40px;
      }

      h1 {
        text-align: center;
        text-decoration: underline;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
        page-break-inside: avoid;
      }

      th,
      td {
        border: 1px solid #000;
        padding: 8px;
        text-align: center;
        word-break: break-word;
        overflow-wrap: break-word;
        page-break-inside: avoid;
      }

      .no-border {
        border: none !important;
      }

      .section-title {
        margin-top: 30px;
        font-weight: bold;
        text-decoration: underline;
        page-break-before: auto;
      }

      .notes,
      .subtext {
        font-size: 11px;
        margin-top: 10px;
      }

      @media print {
        body {
          margin: 20mm;
        }

        .section-title {
          page-break-before: always;
        }

        table,
        tr,
        td,
        th {
          page-break-inside: avoid !important;
          page-break-after: auto;
        }

        @page {
          margin: 20mm;
        }
      }
    </style>
  </head>
  <body>
    <div>
      <h1>Quotation of Elevator</h1>

      <p><strong>Buyer:</strong> <%= customer.clientName %> (<%= customer.companyName %>)</p>
      <p><strong>Project Name:</strong> <%= customer.siteName %></p>
      <p><strong>Email:</strong> <%= customer.email %></p>
      <p><strong>Phone:</strong> <%= customer.phone %></p>
      <p><strong>Address:</strong> <%= customer.address || 'N/A' %></p>
      <p><strong>Date:</strong> <%= new Date().toLocaleDateString() %></p>

      <div class="section-title">1. Product & Price</div>
      <table>
        <thead>
          <tr>
            <th>Product Name</th>
            <th>Machine Room Type</th>
            <th>Shaft Size</th>
            <th>Passenger</th>
            <th>Capacity</th>
            <th>Door Operation</th>
            <th>Door Type</th>
            <th>Opening</th>
            <th>Machine</th>
            <th>Cabin</th>
            <th>Bracket Combination</th>
            <th>Quotation Number</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= product.liftType %></td>
            <td><%= product.ml_mr %></td>
            <td><%= product.shaftSize %></td>
            <td><%= product.passenger %></td>
            <td><%= product.capacity %></td>
            <td><%= product.doorOperation %></td>
            <td><%= product.door_type %></td>
            <td><%= product.opening %></td>
            <td><%= product.machine %></td>
            <td><%= product.cabin %></td>
            <td><%= product.bracket_combination %></td>
            <td><%= product.quotation_number %></td>
          </tr>
        </tbody>
      </table>

      <div class="section-title">2. Floor Details</div>
      <table>
        <thead>
          <tr>
            <th>Floor Type</th>
            <th>Height (mm)</th>
          </tr>
        </thead>
        <tbody>
          <% basementArr.forEach(item => { %>
            <tr><td>Basement</td><td><%= item.height %></td></tr>
          <% }) %>
          <% ground_floorArr.forEach(item => { %>
            <tr><td>Ground Floor</td><td><%= item.height %></td></tr>
          <% }) %>
          <% floorArr.forEach(item => { %>
            <tr><td>Floor</td><td><%= item.height %></td></tr>
          <% }) %>
        </tbody>
      </table>

      <p><strong>Total Floors:</strong> <%= product.total_floor %></p>
      <p><strong>Floor Summary:</strong> <%= product.floor_string %></p>

      <div class="section-title">3. Selected Extras</div>
      <ul>
        <% (product.selectedExtras || []).forEach(extra => { %>
          <li><%= typeof extra === 'string' ? extra : extra.name %></li>
        <% }) %>
      </ul>

      <div class="section-title">4. Extra Fields</div>
      <table>
        <tbody>
          <tr><td>Harness</td><td><%= extrafield.harness || 'N/A' %></td></tr>
          <tr><td>C2C</td><td><%= extrafield.c2c || 'N/A' %></td></tr>
          <tr><td>Machine Base</td><td><%= extrafield.machineBase || 'N/A' %></td></tr>
          <tr><td>Overload Sensor</td><td><%= extrafield.overloadSensor || 'N/A' %></td></tr>
          <tr><td>ARD</td><td><%= extrafield.ARD || 'N/A' %></td></tr>
          <tr><td>UPS</td><td><%= extrafield.UPS || 'N/A' %></td></tr>
        </tbody>
      </table>

      <div class="section-title">5. Notes</div>
      <p><%= product.note || 'N/A' %></p>

      <p><strong>Total Amount:</strong> <!-- dynamic amount here if you have --></p>
      <p><strong>Container No.:</strong> 1*20GP</p>

      <div class="notes">
        <p><strong>Note:</strong> The quotation bases on equipment price terms, excluding installation and training charge.</p>
        <ul>
          <li>$1200/unit</li>
          <li>$180/person/day</li>
          <li>Flight, hotel, board fee, visa (actual)</li>
        </ul>
      </div>

      <div class="section-title">6. Packing</div>
      <p class="subtext">Export packing gluing wooden box</p>

      <div class="section-title">7. Payment Terms</div>
      <p class="subtext">30% T/T in advance & 70% T/T before shipment</p>

      <div class="section-title">8. Delivery Time</div>
      <p class="subtext">45 days from T/T receipt and drawing confirmation</p>

      <div class="section-title">9. Warranty</div>
      <p class="subtext">12 months after delivery (Excl. natural disaster, war, manual damage)</p>

      <div class="section-title">10. Valid Period</div>
      <p class="subtext">60 days</p>

      <div class="section-title">11. Documents with Shipment</div>
      <p class="subtext">Installation guide, electrical plan, packing list, manual (English)</p>
    </div>
  </body>
</html>
